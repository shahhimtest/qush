<nav class="navbar navbar-default navbar-fixed-top">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to root_path, class: 'navbar-brand' do %>
        <%= image_tag "navbar/brand.svg", class: 'img-navbar-brand', alt: 'Qush' %>
      <% end %>
    </div>
    <div id="navbar" class="collapse navbar-collapse">
      <ul class="nav navbar-nav">
        <% if user_signed_in? %>
          <li><%= link_to 'Push', new_message_path %></li>
        <% end %>
      </ul>

      <%= search_form_for search_q, class: 'navbar-form navbar-left' do |f| %>
        <%= f.search_field :name_or_username_cont, class: 'form-control', placeholder: 'Search for user' %>
        <%= f.button :button, class: 'btn btn-default' do %>
          Search
        <% end %>
      <% end %>

      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
          <li>
            <li class="dropdown">
            <%= link_to current_user.name, user_path(current_user), role: 'button', data: { toggle: 'dropdown' }, aria: { haspopup: 'true', expanded: 'false' } %>
            <ul class="dropdown-menu">
              <li><%= link_to 'Profile', user_path(current_user) %></li>
              <li><%= link_to 'Edit', edit_user_path(current_user) %></li>
              <li role="separator" class="divider"></li>
              <li><%= link_to 'Logout', signout_path, method: :delete %></li>
            </ul>
          </li>
        <% else %>
          <li><%= link_to 'Sign In', signin_path %></li>
        <% end %>
      </ul>
    </div><!--/.nav-collapse -->
  </div>
</nav>

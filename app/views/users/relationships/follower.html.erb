<div class="row">
  <div class="col-md-offset-3 col-md-6">
    <div class="panel panel-default">
      <div class="panel-heading">
        <h3 class="panel-title">
          Follower
        </h3>
      </div>

      <% if @follower.empty? %>
        <div class="panel-body">
          No followers.
        </div>
      <% end %>

      <%= render 'list', users: @follower %>

      <% if @follower.total_pages > 1 %>
        <div class="panel-footer text-center">
          <%= will_paginate @follower, renderer: BootstrapPagination::Rails %>
        </div>
      <% end %>
    </div>
  </div>
</div>
